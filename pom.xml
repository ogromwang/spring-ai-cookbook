<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.8</version>
        <relativePath/>
    </parent>
    <groupId>dev.dong4j.ai</groupId>
    <artifactId>spring-ai-cookbook</artifactId>
    <version>${revision}</version>

    <name>spring-ai-cookbook</name>
    <description>Spring AI CookBook</description>
    <packaging>pom</packaging>
    <url>https://github.com/dong4j/spring-ai-cookbook</url>
    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
        </license>
    </licenses>
    <developers>
        <developer>
            <id>dong4j</id>
            <name>dong4j</name>
            <email>dong4j@gmail.com</email>
            <url>https://github.com/dong4j</url>
            <organization>Zeka.Stack</organization>
            <organizationUrl>https://github.com/zeka-stack</organizationUrl>
            <timezone>+08:00</timezone>
        </developer>
    </developers>
    <scm>
        <connection>scm:git:https://github.com/dong4j/spring-ai-cookbook.git</connection>
        <developerConnection>scm:git:git@github.com:dong4j/spring-ai-cookbook.git</developerConnection>
        <tag>HEAD</tag>
        <url>https://github.com/dong4j/spring-ai-cookbook</url>
    </scm>
    <properties>
        <revision>1.0.0-SNAPSHOT</revision>

        <java.version>25</java.version>
        <spring-ai.version>1.1.0</spring-ai.version>
        <google-java-format.version>1.23.0</google-java-format.version>

        <flatten-maven-plugin.version>1.5.0</flatten-maven-plugin.version>
    </properties>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <modules>
        <module>1.spring-ai-started</module>
        <module>2.spring-ai-chat-client</module>
        <module>3.spring-ai-prompts</module>
        <module>4.spring-ai-structured</module>
        <module>5.spring-ai-multimodality</module>
        <module>6.spring-ai-model</module>
        <module>7.spring-ai-model-chat</module>
        <module>8.spring-ai-model-embedding</module>
        <module>9.spring-ai-model-image</module>
        <module>10.spring-ai-model-audio</module>
        <module>11.spring-ai-model-moderation</module>
        <module>12.spring-ai-model-memory</module>
        <module>13.spring-ai-model-tool-calling</module>
        <module>14.spring-ai-mcp</module>
        <module>15.spring-ai-rag</module>
        <module>16.spring-ai-model-evaluation</module>
        <module>17.spring-ai-vector-database</module>
        <module>18.spring-ai-observability</module>
        <module>19.spring-ai-docker</module>
        <module>20.spring-ai-testcontainer</module>
    </modules>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Google Java Format Plugin -->
                <plugin>
                    <groupId>com.github.spotbugs</groupId>
                    <artifactId>spotbugs-maven-plugin</artifactId>
                    <version>4.8.2.2</version>
                </plugin>
                <!-- Checkstyle Plugin -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-checkstyle-plugin</artifactId>
                    <version>3.3.1</version>
                    <configuration>
                        <configLocation>checkstyle.xml</configLocation>
                        <consoleOutput>true</consoleOutput>
                        <failsOnError>true</failsOnError>
                        <linkXRef>false</linkXRef>
                    </configuration>
                    <dependencies>
                        <dependency>
                            <groupId>com.puppycrawl.tools</groupId>
                            <artifactId>checkstyle</artifactId>
                            <version>11.0.1</version>
                        </dependency>
                    </dependencies>
                </plugin>
                <!-- Maven Surefire Plugin -->
                <!-- 配置测试运行时的 JVM 参数，解决 Java 25 下的兼容性问题 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <!--
                            JVM 参数说明：
                            1. @{argLine}: 保留其他插件（如 JaCoCo）可能添加的参数
                            2. -XX:+EnableDynamicAgentLoading: 允许 Mockito 在运行时动态加载 Java agent
                            3. -Xshare:off: 禁用类数据共享（CDS），避免 bootstrap classpath 修改导致的警告
                            4. -enable-native-access=ALL-UNNAMED: 允许未命名模块访问受限的本地方法（消除 jansi 库警告）
                            5. -add-opens: 开放 Java 内部 API 访问权限，避免模块系统限制产生的警告
                               - java.base/sun.nio.ch=ALL-UNNAMED: 开放 sun.nio.ch 包给未命名模块
                               - java.base/java.io=ALL-UNNAMED: 开放 java.io 包给未命名模块
                            注意: sun.misc.Unsafe 的废弃警告来自第三方库（如 guava），无法通过 JVM 参数消除
                            这些配置是 Java 9+ 模块系统下运行测试框架（Mockito、ByteBuddy）和 Maven 依赖库的标准做法
                        -->
                        <argLine>
                            @{argLine}
                            -XX:+EnableDynamicAgentLoading
                            -Xshare:off
                            --enable-native-access=ALL-UNNAMED
                            --add-opens java.base/sun.nio.ch=ALL-UNNAMED
                            --add-opens java.base/java.io=ALL-UNNAMED
                        </argLine>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!-- Google Java Format -->
            <plugin>
                <groupId>com.spotify.fmt</groupId>
                <artifactId>fmt-maven-plugin</artifactId>
                <version>2.29</version>
                <configuration>
                    <filesNamePattern>.*\.java</filesNamePattern>
                    <skipSourceDirectory>false</skipSourceDirectory>
                    <skipTestSourceDirectory>false</skipTestSourceDirectory>
                    <skipSortingImports>false</skipSortingImports>
                    <skipRemovingUnusedImports>false</skipRemovingUnusedImports>
                    <skipReflowingLongStrings>false</skipReflowingLongStrings>
                    <style>aosp</style>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>format</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>com.google.googlejavaformat</groupId>
                        <artifactId>google-java-format</artifactId>
                        <version>1.28.0</version>
                    </dependency>
                </dependencies>
            </plugin>
            <!-- Checkstyle -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <executions>
                    <execution>
                        <id>validate</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>${flatten-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                        <phase>process-resources</phase>
                        <configuration>
                            <updatePomFile>true</updatePomFile>
                            <flattenMode>ossrh</flattenMode>
                            <pomElements>
                                <distributionManagement>remove</distributionManagement>
                                <dependencyManagement>remove</dependencyManagement>
                                <repositories>remove</repositories>
                                <scm>keep</scm>
                                <url>keep</url>
                                <organization>resolve</organization>
                            </pomElements>
                        </configuration>
                    </execution>
                    <execution>
                        <id>clean</id>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                        <phase>clean</phase>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
