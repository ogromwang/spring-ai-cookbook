# 工具调用

工具调用（Tool Calling）允许 AI 模型调用外部函数或服务，实现更强大的功能。

::: code-group

```java [java:定义工具]
@Component
public class WeatherService {
    
    @Tool("获取指定城市的天气信息")
    public String getWeather(@P("城市名称") String city) {
        // 调用天气 API
        return "北京：晴天，25°C";
    }
    
    @Tool("计算两个数字的和")
    public int add(@P("第一个数字") int a, @P("第二个数字") int b) {
        return a + b;
    }
}
```

```java [java:使用工具]
@Autowired
private ChatClient chatClient;

// 工具会自动注册到 ChatClient
String response = chatClient.prompt()
    .user("北京今天天气怎么样？")
    .call()
    .content();
// AI 会自动调用 getWeather("北京") 工具
```

:::

## 参考文档

- [Spring AI Tool Calling 官方文档](https://docs.spring.io/spring-ai/reference/api/tools.html)

## 继续深入

- [[13.spring-ai-model-tool-calling/index|工具调用]]

---
